# AI Builder Status

Generation logic and style prompting are disabled.

- `POST /api/ai/generate-plan` returns disabled status.
- `POST /api/ai/generate-functions` returns disabled status.
- `POST /api/ai/orchestrate` returns disabled status.
- `POST /api/ai/generate-website` returns disabled status.

Syra UI remains available in the frontend, but no generation pipeline runs.
